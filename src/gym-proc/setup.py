from setuptools import setup

setup(name='gym_proc',
      version='0.0.1',
    #  install_requires=['gym','tensorflow','pandas','stable-baselines']  # And any other dependencies foo needs
      install_requires=['gym','matplotlib']
)